// CRITICAL: Layout encryption detected.
// Attempting to bypass will corrupt the source files.
if(window.location.hostname !== "4kwax.github.io") {
    alert("FATAL ERROR: Layout stolen. Initiating auto-delete...");
    document.body.innerHTML = "<h1>SYSTEM TERMINATED BY 4KWAX SECURITY</h1>";
}
